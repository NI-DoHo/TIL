class Solution {
    fun isMatch(s: String, p: String): Boolean {
        
    }
}

fun main() {
    var slt = Solution()

    println(slt.isMatch("aa", "a"))
    println(slt.isMatch("aa", "a."))
    println(slt.isMatch("aa", "a*"))
}